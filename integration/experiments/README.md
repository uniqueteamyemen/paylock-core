# Completed Experiments

This directory groups finished experiment packages by ecosystem name so each package keeps its own notes, checks, failures, and evidence together.

## Stripe

Stripe-related completed experiments are grouped under `stripe/`:

- `digital-goods/`
- `stripe-subscriptions/`

## Medusa

Medusa materials live under `medusa/`.

## Vendure

Vendure materials live under `vendure/`, including the PDF artifact and screenshots referenced by the evidence index.

## Still Active Work

The following are intentionally not moved into this completed-experiments archive because they are still active:

- `checkly/`
- Railway runtime work
- Docker and Docker Hub assets
