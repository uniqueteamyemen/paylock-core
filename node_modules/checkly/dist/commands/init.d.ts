import { BaseCommand } from './baseCommand';
export default class Init extends BaseCommand {
    static description: string;
    static examples: string[];
    static hidden: boolean;
    static coreCommand: boolean;
    static idempotent: boolean;
    static flags: {
        target: import("@oclif/core/interfaces").OptionFlag<string | undefined, import("@oclif/core/interfaces").CustomOptions>;
    };
    run(): Promise<void>;
    private runNewProjectFlow;
    private runAIPath;
    private runManualPath;
    private runExistingProjectFlow;
    private loadStarterPrompt;
    private installExplicitTargetSkill;
    private ensureProjectPackageJson;
    private createPackageJson;
    private ensureProjectSetup;
    private runAgentMode;
    private runCiMode;
}
