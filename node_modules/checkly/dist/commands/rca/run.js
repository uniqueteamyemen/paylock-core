"use strict";
var __createBinding = (this && this.__createBinding) || (Object.create ? (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    var desc = Object.getOwnPropertyDescriptor(m, k);
    if (!desc || ("get" in desc ? !m.__esModule : desc.writable || desc.configurable)) {
      desc = { enumerable: true, get: function() { return m[k]; } };
    }
    Object.defineProperty(o, k2, desc);
}) : (function(o, m, k, k2) {
    if (k2 === undefined) k2 = k;
    o[k2] = m[k];
}));
var __setModuleDefault = (this && this.__setModuleDefault) || (Object.create ? (function(o, v) {
    Object.defineProperty(o, "default", { enumerable: true, value: v });
}) : function(o, v) {
    o["default"] = v;
});
var __importStar = (this && this.__importStar) || (function () {
    var ownKeys = function(o) {
        ownKeys = Object.getOwnPropertyNames || function (o) {
            var ar = [];
            for (var k in o) if (Object.prototype.hasOwnProperty.call(o, k)) ar[ar.length] = k;
            return ar;
        };
        return ownKeys(o);
    };
    return function (mod) {
        if (mod && mod.__esModule) return mod;
        var result = {};
        if (mod != null) for (var k = ownKeys(mod), i = 0; i < k.length; i++) if (k[i] !== "default") __createBinding(result, mod, k[i]);
        __setModuleDefault(result, mod);
        return result;
    };
})();
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
const core_1 = require("@oclif/core");
const chalk_1 = __importDefault(require("chalk"));
const authCommand_1 = require("../authCommand");
const flags_1 = require("../../helpers/flags");
const api = __importStar(require("../../rest/api"));
const errors_1 = require("../../rest/errors");
const rca_1 = require("../../formatters/rca");
class RcaRun extends authCommand_1.AuthCommand {
    static hidden = false;
    static readOnly = false;
    static idempotent = false;
    static description = 'Trigger a root cause analysis for an error group.';
    static flags = {
        'error-group': core_1.Flags.string({
            char: 'e',
            description: 'The error group ID to analyze.',
            required: true,
        }),
        'watch': core_1.Flags.boolean({
            char: 'w',
            description: 'Wait for the analysis to complete and display the result.',
            default: false,
        }),
        'output': (0, flags_1.outputFlag)({ default: 'detail' }),
    };
    async run() {
        const { flags } = await this.parse(RcaRun);
        this.style.outputFormat = flags.output;
        try {
            // Fetch the error group to get the checkId for navigation hints
            const { data: errorGroup } = await api.errorGroups.get(flags['error-group']);
            // Trigger the RCA
            const { data: { id: rcaId } } = await api.rca.trigger(flags['error-group']);
            const pendingInfo = {
                rcaId,
                errorGroupId: flags['error-group'],
                checkId: errorGroup.checkId,
            };
            // If not watching, show pending state and exit
            if (!flags.watch || flags.output === 'json' || flags.output === 'md') {
                if (flags.watch && flags.output !== 'detail') {
                    process.stderr.write(`--watch is not supported with --output ${flags.output}, ignoring\n`);
                }
                const fmt = flags.output === 'json' ? 'json' : flags.output === 'md' ? 'md' : 'terminal';
                this.log((0, rca_1.formatRcaPending)(pendingInfo, fmt));
                return;
            }
            // Watch mode: poll until complete
            this.log(chalk_1.default.bold('Root cause analysis triggered.'));
            this.log(`${chalk_1.default.dim('RCA ID:')}  ${rcaId}`);
            this.log('');
            this.style.actionStart('Waiting for root cause analysis...');
            const rca = await api.rca.pollUntilComplete(rcaId);
            this.style.actionSuccess();
            this.log((0, rca_1.formatRcaCompleted)(rca, 'terminal'));
        }
        catch (err) {
            if (err instanceof errors_1.InadequateEntitlementsError) {
                this.style.longError('Root cause analysis is not available on your current plan.', 'Run `checkly account plan` to check your entitlements.');
                process.exitCode = 1;
                return;
            }
            if (err instanceof errors_1.NotFoundError) {
                this.style.shortError(`Error group not found: ${flags['error-group']}`);
                process.exitCode = 1;
                return;
            }
            this.style.longError('Failed to trigger root cause analysis.', err);
            process.exitCode = 1;
        }
    }
}
exports.default = RcaRun;
//# sourceMappingURL=run.js.map