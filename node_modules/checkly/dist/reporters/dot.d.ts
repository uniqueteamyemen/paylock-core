import AbstractListReporter from './abstract-list';
import { SequenceId } from '../services/abstract-check-runner';
export default class DotReporter extends AbstractListReporter {
    onBegin(checks: Array<{
        check: any;
        sequenceId: SequenceId;
    }>, testSessionId?: string): void;
    onEnd(): void;
    onCheckEnd(sequenceId: SequenceId, checkResult: any): void;
}
