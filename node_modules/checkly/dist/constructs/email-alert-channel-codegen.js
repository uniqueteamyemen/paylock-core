"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.EmailAlertChannelCodegen = void 0;
const codegen_1 = require("./internal/codegen");
const sourcegen_1 = require("../sourcegen");
const alert_channel_codegen_1 = require("./alert-channel-codegen");
const construct = 'EmailAlertChannel';
class EmailAlertChannelCodegen extends codegen_1.Codegen {
    describe(resource) {
        return `Email Alert Channel: ${resource.config.address}`;
    }
    prepare(logicalId, resource, context) {
        const { address } = resource.config;
        const prefix = address.split('@')[0];
        const filename = context.filePath('resources/alert-channels/email', prefix, {
            unique: true,
        });
        context.registerAlertChannel(resource.id, `${prefix} email`, this.program.generatedConstructFile(filename.fullPath));
    }
    gencode(logicalId, resource, context) {
        const { id, file } = context.lookupAlertChannel(resource.id);
        file.namedImport(construct, 'checkly/constructs');
        file.section((0, sourcegen_1.decl)(id, builder => {
            builder.variable((0, sourcegen_1.expr)((0, sourcegen_1.ident)(construct), builder => {
                builder.new(builder => {
                    builder.string(logicalId);
                    builder.object(builder => {
                        builder.string('address', resource.config.address);
                        (0, alert_channel_codegen_1.buildAlertChannelProps)(builder, resource);
                    });
                });
            }));
            builder.export();
        }));
    }
}
exports.EmailAlertChannelCodegen = EmailAlertChannelCodegen;
//# sourceMappingURL=email-alert-channel-codegen.js.map