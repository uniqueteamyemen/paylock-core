import { Value, GeneratedFile } from '../sourcegen';
import { RetryStrategy } from './retry-strategy';
export type RetryStrategyResource = RetryStrategy;
export declare function valueForRetryStrategy(genfile: GeneratedFile, strategy?: RetryStrategyResource | null): Value;
