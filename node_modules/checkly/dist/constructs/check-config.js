"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.makeConfigDefaultsGetter = makeConfigDefaultsGetter;
function makeConfigDefaultsGetter(...defaults) {
    const ok = defaults.filter(value => value !== undefined);
    function get(key) {
        for (const config of ok) {
            // Older TS seems to need this check.
            if (config === undefined) {
                continue;
            }
            const value = config[key];
            if (value !== undefined) {
                return value;
            }
        }
    }
    return get;
}
//# sourceMappingURL=check-config.js.map