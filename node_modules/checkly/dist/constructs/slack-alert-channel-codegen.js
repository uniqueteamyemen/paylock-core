"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.SlackAlertChannelCodegen = void 0;
const codegen_1 = require("./internal/codegen");
const sourcegen_1 = require("../sourcegen");
const alert_channel_codegen_1 = require("./alert-channel-codegen");
const construct = 'SlackAlertChannel';
class SlackAlertChannelCodegen extends codegen_1.Codegen {
    describe(resource) {
        if (resource.config.channel) {
            return `Slack Alert Channel: ${resource.config.channel}`;
        }
        return 'Slack Alert Channel';
    }
    prepare(logicalId, resource, context) {
        const { channel, url } = resource.config;
        const last4Chars = url.slice(-4);
        const fallbackName = `slack-${last4Chars}`;
        const filename = context.filePath('resources/alert-channels/slack', channel || fallbackName, {
            unique: true,
        });
        context.registerAlertChannel(resource.id, channel ? `${channel} slack` : fallbackName, this.program.generatedConstructFile(filename.fullPath));
    }
    gencode(logicalId, resource, context) {
        const { id, file } = context.lookupAlertChannel(resource.id);
        file.namedImport(construct, 'checkly/constructs');
        const { config } = resource;
        file.section((0, sourcegen_1.decl)(id, builder => {
            builder.variable((0, sourcegen_1.expr)((0, sourcegen_1.ident)(construct), builder => {
                builder.new(builder => {
                    builder.string(logicalId);
                    builder.object(builder => {
                        builder.string('url', config.url);
                        if (config.channel) {
                            builder.string('channel', config.channel);
                        }
                        (0, alert_channel_codegen_1.buildAlertChannelProps)(builder, resource);
                    });
                });
            }));
            builder.export();
        }));
    }
}
exports.SlackAlertChannelCodegen = SlackAlertChannelCodegen;
//# sourceMappingURL=slack-alert-channel-codegen.js.map