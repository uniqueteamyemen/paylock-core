"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.BrowserCheckBundle = void 0;
class BrowserCheckBundle {
    browserCheck;
    script;
    scriptPath;
    dependencies;
    // For snapshots, we first store `rawSnapshots` with the path to the file.
    // The `snapshots` field is set later (with a `key`) after these are uploaded to storage.
    rawSnapshots;
    snapshots;
    constructor(browserCheck, props) {
        this.browserCheck = browserCheck;
        this.script = props.script;
        this.scriptPath = props.scriptPath;
        this.dependencies = props.dependencies;
        this.rawSnapshots = props.rawSnapshots;
    }
    synthesize() {
        return {
            ...this.browserCheck.synthesize(),
            script: this.script,
            scriptPath: this.scriptPath,
            dependencies: this.dependencies,
            snapshots: this.snapshots,
        };
    }
}
exports.BrowserCheckBundle = BrowserCheckBundle;
//# sourceMappingURL=browser-check-bundle.js.map