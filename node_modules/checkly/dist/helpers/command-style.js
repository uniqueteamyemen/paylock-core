"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.CommandStyle = void 0;
const chalk_1 = __importDefault(require("chalk"));
const core_1 = require("@oclif/core");
const wrap_1 = require("./wrap");
const log_symbols_1 = __importDefault(require("log-symbols"));
const textWrapOptions = {
    prefix: '  ',
    length: 78,
};
const errorWrapOptions = {
    ...textWrapOptions,
    length: Infinity,
};
class CommandStyle {
    c;
    outputFormat;
    constructor(command) {
        this.c = command;
    }
    actionStart(message) {
        if (this.c.fancy) {
            core_1.ux.action.start(message, undefined, { stdout: true });
        }
    }
    actionSuccess() {
        if (this.c.fancy) {
            core_1.ux.action.stop(`✅`);
            this.c.log();
        }
    }
    actionFailure() {
        if (this.c.fancy) {
            core_1.ux.action.stop(`❌`);
            this.c.log();
        }
    }
    comment(message) {
        this.c.log(chalk_1.default.cyan((0, wrap_1.wrap)(message, { ...textWrapOptions, prefix: '// ' })));
        this.c.log();
    }
    longSuccess(title, message) {
        this.c.log(`${log_symbols_1.default.success} ${title}`);
        this.c.log();
        this.c.log((0, wrap_1.wrap)(message, textWrapOptions));
        this.c.log();
    }
    shortSuccess(message) {
        this.c.log(`${log_symbols_1.default.success} ${message}`);
        this.c.log();
    }
    longInfo(title, message) {
        this.c.log(`${log_symbols_1.default.info} ${title}`);
        this.c.log();
        this.c.log((0, wrap_1.wrap)(message, textWrapOptions));
        this.c.log();
    }
    shortInfo(message) {
        this.c.log(`${log_symbols_1.default.info} ${message}`);
        this.c.log();
    }
    longWarning(title, message) {
        this.c.log(`${log_symbols_1.default.warning} ${title}`);
        this.c.log();
        this.c.log(chalk_1.default.yellow(this.#formatDescription(message)));
        this.c.log();
    }
    shortWarning(message) {
        this.c.log(`${log_symbols_1.default.warning} ${chalk_1.default.yellow(message)}`);
        this.c.log();
    }
    longError(title, message) {
        if (this.outputFormat === 'json') {
            this.c.log(JSON.stringify({ error: title, detail: this.#plainDescription(message) }));
            return;
        }
        this.c.log(`${log_symbols_1.default.error} ${title}`);
        this.c.log();
        this.c.log(chalk_1.default.red(this.#formatDescription(message)));
        this.c.log();
    }
    shortError(message) {
        if (this.outputFormat === 'json') {
            this.c.log(JSON.stringify({ error: message }));
            return;
        }
        this.c.log(`${log_symbols_1.default.error} ${chalk_1.default.red(message)}`);
        this.c.log();
    }
    fatal(message) {
        this.c.log(chalk_1.default.red(message));
        this.c.log();
    }
    #formatDescription(description) {
        if (typeof description === 'string') {
            return (0, wrap_1.wrap)(description, textWrapOptions);
        }
        const { message } = description;
        return (0, wrap_1.wrap)(message, errorWrapOptions);
    }
    #plainDescription(description) {
        return typeof description === 'string' ? description : description.message;
    }
}
exports.CommandStyle = CommandStyle;
//# sourceMappingURL=command-style.js.map