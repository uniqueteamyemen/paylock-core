"use strict";
var __importDefault = (this && this.__importDefault) || function (mod) {
    return (mod && mod.__esModule) ? mod : { "default": mod };
};
Object.defineProperty(exports, "__esModule", { value: true });
exports.runSkillInstallStep = runSkillInstallStep;
exports.refreshSkill = refreshSkill;
const path_1 = require("path");
const chalk_1 = __importDefault(require("chalk"));
const prompts_1 = __importDefault(require("prompts"));
const install_1 = require("../../commands/skills/install");
const cli_mode_1 = require("../cli-mode");
const prompts_helpers_1 = require("./prompts-helpers");
function runSkillInstallStep(log) {
    const mode = (0, cli_mode_1.detectCliMode)();
    if (mode === 'ci') {
        return Promise.resolve({ installed: false, platform: null, targetPath: null });
    }
    if (mode === 'agent') {
        return runAgentInstall(log);
    }
    return runInteractiveInstall(log);
}
async function runAgentInstall(log) {
    const operator = (0, cli_mode_1.detectOperator)();
    const platform = cli_mode_1.OPERATOR_TO_PLATFORM[operator];
    if (!platform) {
        return { installed: false, platform: null, targetPath: null };
    }
    const targetDir = install_1.PLATFORM_TARGETS[platform];
    if (!targetDir) {
        return { installed: false, platform: null, targetPath: null };
    }
    try {
        const content = await (0, install_1.readSkillFile)();
        const targetPath = await (0, install_1.writeSkillToTarget)(targetDir, content);
        log(chalk_1.default.green(`Installed Checkly skill for ${(0, install_1.formatPlatformName)(platform)} at ${targetPath}`));
        return { installed: true, platform, targetPath };
    }
    catch (error) {
        log(chalk_1.default.red(`Could not install skill: ${error.message || String(error)}`));
        return { installed: false, platform: null, targetPath: null };
    }
}
async function runInteractiveInstall(log) {
    log('');
    const { install } = await (0, prompts_1.default)({
        type: 'confirm',
        name: 'install',
        message: 'Install the Checkly skill for your AI coding agent?',
        initial: true,
    }, {
        onCancel: (0, prompts_helpers_1.makeOnCancel)(log),
    });
    if (!install) {
        return { installed: false, platform: null, targetPath: null };
    }
    const targetDir = await (0, install_1.promptForPlatformTarget)((0, prompts_helpers_1.makeOnCancel)(log));
    if (!targetDir) {
        return { installed: false, platform: null, targetPath: null };
    }
    // Determine which platform was selected (if any) by reverse-looking up the directory
    const platformEntry = Object.entries(install_1.PLATFORM_TARGETS).find(([, dir]) => dir === targetDir);
    const platform = platformEntry ? platformEntry[0] : null;
    try {
        const content = await (0, install_1.readSkillFile)();
        const targetPath = await (0, install_1.writeSkillToTarget)(targetDir, content);
        log(chalk_1.default.green(`Installed Checkly skill at ${targetPath}`));
        return {
            installed: true,
            platform,
            targetPath,
        };
    }
    catch (error) {
        log(chalk_1.default.red(`Could not install skill: ${error.message || String(error)}`));
        return { installed: false, platform: null, targetPath: null };
    }
}
async function refreshSkill(skillPath, log) {
    try {
        const targetDir = (0, path_1.relative)(process.cwd(), (0, path_1.dirname)(skillPath));
        const content = await (0, install_1.readSkillFile)();
        const targetPath = await (0, install_1.writeSkillToTarget)(targetDir, content);
        return { installed: true, targetPath };
    }
    catch {
        log(chalk_1.default.dim('  Could not update skill file.'));
        return { installed: false, targetPath: null };
    }
}
//# sourceMappingURL=skill-install.js.map