"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.loadPromptTemplate = loadPromptTemplate;
const promises_1 = require("fs/promises");
const path_1 = require("path");
const PROMPTS_DIR = (0, path_1.join)(__dirname, '../../ai-context/onboarding-prompts');
async function loadPromptTemplate(templateName, variables) {
    const templatePath = (0, path_1.join)(PROMPTS_DIR, `${templateName}.md`);
    let content = await (0, promises_1.readFile)(templatePath, 'utf8');
    for (const [key, value] of Object.entries(variables)) {
        content = content.replaceAll(`{{${key}}}`, value);
    }
    return content.trim();
}
//# sourceMappingURL=template-prompt.js.map