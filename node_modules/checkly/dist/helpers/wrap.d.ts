export interface WrapOptions {
    length?: number;
    prefix?: string;
    trimPrefix?: boolean;
}
export declare function wrap(content: string, { length, prefix, trimPrefix }?: WrapOptions): string;
