export interface WalkUpOptions extends LineageOptions {
    isDir?: boolean;
}
export declare function walkUp(filePath: string, find: (dirPath: string) => Promise<boolean>, options?: WalkUpOptions): Promise<boolean>;
export interface LineageOptions {
    root?: string;
}
export declare function lineage(path: string, options?: LineageOptions): Generator<string, void>;
