"use strict";
Object.defineProperty(exports, "__esModule", { value: true });
exports.quickRangeValues = void 0;
const constants_1 = require("../constants");
exports.quickRangeValues = [
    'last24Hours', 'last7Days', 'last30Days', 'thisWeek', 'thisMonth', 'lastWeek', 'lastMonth',
];
// AGENTIC is intentionally absent: agentic checks have no per-check analytics
// endpoint, so callers fall through to the `if (!pathSegment)` branch below.
const checkTypeToPath = {
    [constants_1.CheckTypes.API]: 'api-checks',
    [constants_1.CheckTypes.BROWSER]: 'browser-checks',
    [constants_1.CheckTypes.PLAYWRIGHT]: 'playwright-checks',
    [constants_1.CheckTypes.MULTI_STEP]: 'multistep-checks',
    [constants_1.CheckTypes.HEARTBEAT]: 'heartbeat-checks',
    [constants_1.CheckTypes.TCP]: 'tcp-checks',
    [constants_1.CheckTypes.ICMP]: 'icmp',
    [constants_1.CheckTypes.DNS]: 'dns',
    [constants_1.CheckTypes.URL]: 'url-monitors',
    [constants_1.CheckTypes.AGENTIC]: 'agentic-checks',
};
// Default aggregated metrics per check type
const defaultMetrics = {
    [constants_1.CheckTypes.API]: ['availability', 'responseTime_avg', 'responseTime_p50', 'responseTime_p95', 'responseTime_p99'],
    [constants_1.CheckTypes.BROWSER]: ['availability', 'LCP_avg', 'CLS_avg', 'TBT_avg', 'responseTime_avg', 'responseTime_p95'],
    [constants_1.CheckTypes.PLAYWRIGHT]: ['availability', 'LCP_avg', 'CLS_avg', 'TBT_avg', 'responseTime_avg', 'responseTime_p95'],
    [constants_1.CheckTypes.MULTI_STEP]: ['availability', 'responseTime_avg', 'responseTime_p50', 'responseTime_p95', 'responseTime_p99'],
    [constants_1.CheckTypes.URL]: ['availability', 'responseTime_avg', 'responseTime_p50', 'responseTime_p95', 'responseTime_p99'],
    [constants_1.CheckTypes.TCP]: ['availability', 'total_avg', 'total_p50', 'total_p95', 'total_p99'],
    [constants_1.CheckTypes.DNS]: ['availability', 'total_avg', 'total_p50', 'total_p95', 'total_p99'],
    [constants_1.CheckTypes.ICMP]: ['availability', 'packetLoss_avg', 'latencyAvg_avg', 'latencyAvg_p50', 'latencyAvg_p95', 'latencyAvg_p99'],
    [constants_1.CheckTypes.HEARTBEAT]: ['availability'],
    [constants_1.CheckTypes.AGENTIC]: ['availability'],
};
class Analytics {
    api;
    constructor(api) {
        this.api = api;
    }
    async get(checkId, checkType, options = {}) {
        const pathSegment = checkTypeToPath[checkType];
        if (!pathSegment) {
            throw new Error(`Unsupported check type: ${checkType}`);
        }
        const metrics = options.metrics ?? defaultMetrics[checkType] ?? ['availability'];
        const params = {
            metrics: metrics.join(','),
            quickRange: options.quickRange || 'last24Hours',
        };
        if (options.groupBy)
            params.groupBy = options.groupBy;
        if (options.filterByStatus)
            params.filterByStatus = options.filterByStatus;
        const response = await this.api.get(`/v1/analytics/${pathSegment}/${checkId}`, {
            params,
        });
        // Attach which metrics were requested so formatters can filter/order
        response.data.requestedMetrics = metrics;
        return response;
    }
}
exports.default = Analytics;
//# sourceMappingURL=analytics.js.map