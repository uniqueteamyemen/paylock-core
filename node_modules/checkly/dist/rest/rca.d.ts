import type { AxiosInstance } from 'axios';
import type { RootCauseAnalysis } from './error-groups';
export interface TriggerRcaResponse {
    id: string;
}
declare class Rca {
    api: AxiosInstance;
    constructor(api: AxiosInstance);
    trigger(errorGroupId: string): Promise<import("axios").AxiosResponse<TriggerRcaResponse, any, {}>>;
    get(id: string): Promise<import("axios").AxiosResponse<RootCauseAnalysis, any, {}>>;
    pollUntilComplete(id: string): Promise<RootCauseAnalysis>;
}
export default Rca;
