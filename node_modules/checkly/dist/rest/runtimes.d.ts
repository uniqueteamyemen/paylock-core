import type { AxiosInstance } from 'axios';
import { Runtime } from '../runtimes';
declare class Runtimes {
    api: AxiosInstance;
    constructor(api: AxiosInstance);
    getAll(): Promise<Runtime[]>;
    get(runtimeId: string): Promise<Runtime>;
}
export default Runtimes;
