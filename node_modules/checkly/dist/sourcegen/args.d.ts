import { Output } from './output';
import { Value } from './value';
export declare class ArgumentsValue extends Value {
    value: Value[];
    constructor(value: Value[]);
    render(output: Output): void;
}
