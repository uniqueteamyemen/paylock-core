import { Check } from '../constructs/check';
import { RunLocation } from '../services/abstract-check-runner';
export interface Reporter {
    onBeginStatic(): void;
    onBegin(testSessionId: string, testResultIds?: {
        [key: string]: string;
    }): void;
    onEnd(): void;
    onCheckEnd(checkResult: any): void;
    onError(err: Error): void;
}
export type ReporterType = 'list' | 'dot' | 'ci' | 'github';
export declare const createReporters: (types: ReporterType[], runLocation: RunLocation, checks: Array<Check>, verbose: boolean) => Reporter[];
